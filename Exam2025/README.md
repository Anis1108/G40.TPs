# Correction – Examen Python G40 (2025)

## QCM

### 1. Le langage Python

a) Le langage Python est un langage  
- Interprété

b) Un script écrit en Python  
- Peut tourner sur n’importe quelle machine grâce à la machine virtuelle Python

c) PiP est un programme qui permet  
- D’installer et de supprimer des librairies Python sur une machine

d) Python dans sa version 3  
- Est un langage typé

e) Un script écrit en Python  
- Dépend fortement de la version de Python avec laquelle il est écrit


---

### 2. Structures de données et fonctions

a) Les chaînes de caractères sont  
- Immuables et homogènes dans leur contenu

b) Pour récupérer les deux derniers éléments de la liste `[1,2,3,4,5,6]`  
- `ma_liste[-2:]`

c) Résultat de  
`ma_liste = [1,3,1,5,5,7,11,13]`  
`mon_set = set(ma_liste)`  

- `{1,3,5,7,11,13}`

d) Les annotations dans le langage Python permettent  
- D’enregistrer des définitions des variables

e) Résultat du programme  
- Rien n’est affiché puisqu’on ne fait que définir des fonctions et on n’en exécute aucune


---

### 3. Programmation orientée objet

a) Python est un langage  
- Hybride

b) Python permet de déclarer des membres privés dans une classe  
- En précédant le nom par le symbole `_`

c) Python permet  
- Un héritage multiple

d) Pour construire une nouvelle instance d’une classe  
- On utilise la fonction `__init__`

e) La fonction `__del__` permet de  
- Décrémenter le compteur mémoire de l’objet (gestion par le garbage collector)
